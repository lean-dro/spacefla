{
  "name": "site",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "cd site && nodemon node app.js",
    "dev": "cd site && npm run dev",
    "postinstall": "cd site && npm install"
  },
  "dependencies": {
    "nodemon": "^2.0.22"
  }
}
